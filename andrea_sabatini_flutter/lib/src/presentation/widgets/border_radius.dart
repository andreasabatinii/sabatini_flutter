import 'package:andrea_sabatini_flutter/src/presentation/widgets/grid.dart';
import 'package:flutter/material.dart';

BorderRadius br(double radius) => BorderRadius.circular(radius);

//devo scirvere solo brM. Il valore già lo prende da Grid.medium
BorderRadius get br8 => BorderRadius.circular(BorRadius.medium);
BorderRadius get br6 => BorderRadius.circular(BorRadius.small);
BorderRadius get br4 => BorderRadius.circular(BorRadius.xssmall);
